<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:webflow="http://www.springframework.org/schema/webflow-config"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">

    <!-- start this flow from:
        http://localhost:8080/mvc/flow -->

    <!-- in jsp we can use ${flowExecutionUrl} to advance -->
    <!-- ex: <a href="${flowExecutionUrl}&_eventId=next">Next</a> -->

    <!-- Note: the first view is the default for a web-flow -->

    <!-- let InternalResourceViewResolver figure out view -->
	<view-state id="home"  view="home" >
        <transition on="next" to="step2"/>
    </view-state>

    <view-state id="step2"  view="step2" />
	
</flow>